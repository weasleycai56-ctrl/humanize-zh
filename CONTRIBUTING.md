# Contributing to Humanize ZH

感谢你帮助改善中文写作工具。这个项目欢迎规则修正、边界反例、测试语料、文档和工具改进。

## 贡献原则

- 把规则写成可观察的编辑问题，不把词语当作 AI 作者身份标签。
- 中文表达有地区、行业、代际和个人差异；规则必须说明合理使用边界。
- 不提交私人聊天、未公开稿件、付费内容、医疗记录或其他无权公开的文本。
- 语料应为你原创、已获明确许可，或使用兼容许可证并附准确来源。
- 示例中不得包含真实密钥、账号、个人联系方式或可识别个人的信息。
- 改写不能虚构事实、数字、引用、体验或社会证明。

## 提交规则提案

请使用 “Rule proposal” issue 模板，并提供：

1. 它影响清晰度、可信度或自然节奏的原因；
2. 至少两个正例；
3. 至少一个看似命中、实际合理的反例；
4. 建议的诊断和改写方法；
5. 文本来源与许可说明。

单个流行词或单篇 AI 输出不足以建立新规则。优先扩展现有规则边界，只有编辑问题确实不同才新增稳定编号。

## 本地开发

需要 Python 3.10+，无第三方依赖：

```bash
python3 -m unittest discover -s tests -v
python3 scripts/public_repo_audit.py
python3 /path/to/skill-creator/scripts/quick_validate.py .
```

最后一项是 Codex skill-creator 提供的验证器；没有该工具时可跳过，但请人工检查 `SKILL.md` frontmatter。

修改规则时同步检查：

- `references/chinese-ai-tells.md` 的定义、后果和边界；
- `scripts/audit_text.py` 的确定性候选规则（如适用）；
- `tests/fixtures/` 的正例、反例或保留项；
- `CHANGELOG.md` 的 `Unreleased` 部分。

确定性脚本不必覆盖 Skill 能理解的每种语义模式。正则应偏保守、可解释，并避免为了召回率制造大量误报。

## Pull request

- 一次 PR 解决一个主题；
- 说明用户可观察的变化，而不是只列文件名；
- 添加或更新有意义的测试；
- 确认没有提交第三方机密、私人数据或许可证不明的内容；
- 不要批量改写无关文档或重新编号现有规则。

提交贡献即表示你有权提供该内容，并同意它按本项目 MIT License 发布。

## 评审标准

维护者会重点检查：真实编辑价值、中文语境、误报边界、事实保留、隐私与版权、向后兼容性，以及文档是否足以让他人维护。礼貌但不同意某项规则是正常的；请围绕可复现示例讨论。
