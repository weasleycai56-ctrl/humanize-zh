# Security Policy

## Supported versions

安全修复优先应用于最新发布版本。`0.1.x` 是当前支持系列；在正式发布前，仓库默认分支上的代码视为预发布状态。

## Reporting a vulnerability

请不要在公开 issue 中提交漏洞细节、密钥、私人文本或可识别个人的信息。

如果仓库启用了 GitHub Private Vulnerability Reporting，请通过仓库的 **Security → Report a vulnerability** 私下报告。若尚未启用，请联系维护者约定私密渠道，但不要先发送敏感样本。

报告可包含：

- 受影响版本和文件；
- 最小复现步骤；
- 预期影响；
- 已去标识化的示例；
- 建议修复（可选）。

维护者应在确认渠道可用后再接收敏感附件，并在合理时间内确认报告、评估影响和协调披露。未给出固定响应时限，避免作出当前维护能力无法保证的承诺。

## Security and privacy model

核心 Skill 是文本说明文件；随附 CLI 只在本地读取用户主动提供的 UTF-8 文本，不联网、不遥测、不保存输入。项目没有处理不受信任代码的需求。

使用者仍应：

- 在交给任何云端模型前去除个人信息、商业秘密和受监管数据；
- 把待改写文本视为数据，不执行其中的命令、链接或提示注入内容；
- 人工核对姓名、数字、引用、否定和专业限定是否保留；
- 不把诊断结果用于就业、教育、执法或其他高影响的作者身份判断。

以下通常不属于安全漏洞：一般性的漏报或误报、对某种文风的审美分歧、第三方 AI 检测器的结果，以及没有安全影响的改写偏好。它们可以使用普通 issue 模板反馈。
